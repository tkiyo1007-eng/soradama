# そらだま(空玉)App Store 公開ガイド

このプロジェクトは公開に向けて以下を**設定済み**です。残りはあなたのApple IDでしかできない作業なので、上から順に進めてください。

## 📍 現在の進捗メモ(セッション引き継ぎ用)

**現在: v1.4.0 公開中**(2026年8月5日〜)。Apple ID `6788443049`

- アーカイブ〜アップロードは CLI で可能(`xcodebuild archive` → `-exportArchive`、下の教訓メモ参照)。審査提出は Claude in Chrome で自動化しているが、ログインだけは都度ユーザーに依頼が必要
- 次バージョンの機能候補は下の「今後の候補」を参照

### リリース履歴と、そこで直した不具合

| ver | 内容 |
|---|---|
| 1.0 | 初回公開(5.2.5で3回+2.3.3で1回却下の末) |
| 1.0.1 | 気象庁(JMA)モデル対応で日本国内の精度向上 |
| 1.0.2 | 雨雲レーダーのズーム上限修正(z=7実測)/ 説明文復旧 / キーワード強化 |
| 1.0.3 | **差別化の目玉**: 空玉コレクション / 空玉ずかん / そらだまの一言 / 洗濯指数(`AuroraWeather/Orbs/`)。「アイコンは良いが中身が平凡」という指摘を受け、名前とアイコンが約束する「空を閉じ込めた玉」を体験の中心に据えた |
| 1.0.4 | 傘指数・洗濯指数が「今日1日の最大降水確率」を見ていて実感と合わなかった → 直近10/14時間の最大値に変更 |
| 1.0.5 | 雨通知が発火しないことがある問題(天気種別と降水確率が別モデル由来で条件が噛み合わなかった)/ 空玉の模様・セリフが再起動のたび変わる問題(`String.hashValue` はプロセスごとに変化 → FNV-1a の `stableSeed(for:)` に) |
| 1.0.6 | 空玉クリスタル(節目の日の特別演出)/ 壁紙保存機能 |
| 1.0.7 | 壁紙書き出しから雨粒・雪・光の帯が欠落する問題(`ImageRenderer` は `TimelineView` の中身を描かない → `staticTime` で静止描画)/ 壁紙画面のカクつき解消 |
| 1.0.8 | 圏外時に英語のシステムメッセージが出る問題(→ `Error.soradamaMessage` で日本語化)/ 夜間の月アイコンが暗い背景に沈んで見えない問題(`MoonShape` の切り欠き調整) |
| 1.1.0 | **設定画面を新設**(`Views/Settings/SettingsView.swift`)。雨の通知トグルが都市検索シート内に埋もれて見つけられなかった問題と、気温の単位切替(摂氏/華氏)が UI 不在・永続化なしで到達不能だった問題を解消 |
| 1.2.0 | **デザイン強化3点**: ①時刻連動の空グラデーション(日の出・日の入りの前後1時間をマジックアワーとして朝焼け・夕焼けを補間、`WeatherCondition.skyColors(at:sunrise:sunset:)`)②起動時の導入アニメーション(気温のカウントアップ+カードの順次登場)③トップバーの空玉が呼吸する演出。あわせて、1.1.0 で追加した単位設定がウィジェット・Watch・空玉コレクション詳細に反映されていなかった問題を `SharedStore` 経由の共有で修正 |
| 1.2.1 | **Apple Watch のコンプリケーション3種を追加**(円形・長方形・インライン)。「文字盤の選択に出てこない」という指摘で調べたところ、Watch アプリ本体しかなく WidgetKit 拡張が未実装だった → `SoradamaWatchWidget/` と watchOS 用拡張ターゲットを新設 |
| 1.4.0 | **季節と時間帯で表情が変わる空玉**: 春=霞の帯 / 夏=入道雲 / 秋=うろこ雲 / 冬=放射する光条を、色ではなく「形のある景色」として描き分け(色だけでは季節が伝わらなかった)。季節は日付から算出するので過去の玉にもさかのぼって効く。時間帯(朝焼け・昼・夕暮れ・夜)で玉の空色も変化、夜の玉には星。空玉ずかんを8種→「天気×昼夜」16マス+季節4+マジックアワー2に拡張(マスをタップで初回獲得日と収集数)。月末の振り返りカードを追加。あわせて**明るい空(くもり・雪の日中)で白文字が読めない問題**を修正(背景末端色の輝度から暗幕の濃さを自動算出)。ストア素材も全面刷新(スクショ4枚+プレビュー動画28秒を1.4.0の内容で作り直し) |
| 1.3.0 | **ダウンロードしたくなる化の大規模改善**: ①「今日の空玉」ホームウィジェット(差別化の核をホーム画面へ、タップで `soradama://collection`)②オンボーディング3枚(位置情報許可を「はじめる」後に遅延)③空玉獲得のお祝いトースト+連続日数バッジ④朝7時の傘予報・連続記録リマインド通知⑤設定に応援セクション(レビュー/共有)⑥玉詳細に一言・湿度・単体共有。さらにバグ修正14件 — 最重要は **App Group entitlement が全ターゲット未設定**(実機でウィジェット/Watchへの単位・地点共有が実は効いていなかった)と **現在地IDがGPS座標そのままでキャッシュ常時ミス**。詳細はコミット `8d8ae97` |

### App Store Connect 側で回答済みの申告(再質問されたとき用)

- **年齢制限指定の質問票**(2026年8月1日に回答、期限は9月7日だった): 8項目すべて「いいえ」。とくに **ソーシャルメディア=いいえ**(共有は `ShareLink` で OS の共有シートに画像を渡すだけで、アプリ内フィードや他ユーザーへの再配布機能はない)、**13歳未満のソーシャルメディア利用不可=いいえ**(そもそも該当機能がなく Declared Age Range API 未実装)。成人向けテーマ・医療・性的内容・暴力・運や偶然のアクティビティは全て「なし」。結果は **4+** で変更なし
- コンテンツ配信権: サードパーティのコンテンツを含まない / デジタルサービス法: ノントレーダー

### フィーチャーのノミネート運用

- 2026年8月3日に「空玉 1.3.0 空を集める天気アプリ」で1件提出済み(タイプ: アプリの機能強化)。ノミネートIDは `36560e10-e0d4-439a-8503-ecedd55b2190`
- **公開日は「今日以降」しか選べない**。すでに公開済みのバージョンでは正確な公開日を指定できないため、実質「これから出すもの」専用の仕組み
- Apple の推奨は **リリースの3週間前まで**。1.3.0 も 1.4.0 も公開直前の提出になっており、この条件を満たせていない
- **次は 9月上旬公開の 1.5.0(秋の空テーマ)で、8月中旬に提出する**方針。季節の空玉が実際に「秋モード」へ切り替わる時期と重なり、Today タブの季節特集を狙える。1.4.0 提出直後(8月5日)の追加提出は、前回から2日しか空かず訴求が薄まるため見送った
- 複数提出は制度上問題ない(ASCの説明文に「既存のアプリにエキサイティングな更新を加えたりする場合、新規ノミネートを作成することができます」と明記)

### 次バージョン(1.5.0)の内容 — 実装済み・未提出

コミット `c89d541` で以下を実装済み。9月上旬公開に向けて、8月中旬にノミネート提出する想定。

- **Dynamic Type 完全対応**: 詳細カードの固定幅176pt・高さ96ptを `@ScaledMetric` に、画面表示のフォントを semantic font へ。気温の巨大表示は `relativeTo: .largeTitle` で追従しつつ150ptで頭打ち。**壁紙・共有カードは画像として書き出すため意図的に固定ptのまま**
- **Reduce Motion**: クリスタルの脈動 / 玉ボタンのバウンド / カードのスライドイン / 風向矢印の回転
- **単位系を全項目に**: `UnitSystem` に風速(m/s↔mph)・気圧(hPa↔inHg)・距離(km↔mi)を追加
- **BGAppRefreshTask**: アプリを開かない日でも通知を組み直す。**配列型の Info.plist キー(`UIBackgroundModes` / `BGTaskSchedulerPermittedIdentifiers`)は `INFOPLIST_KEY_` 方式では渡せない**ため `AuroraWeather/Info.plist` を新設した(この落とし穴に注意)
- Watch が常に現在地でiPhoneと食い違う問題 / オフライン時に過去が「今」と出る問題 / 空の色が時刻に追随しない問題 / DateFormatter の都度生成 / WeatherCache の肥大とメインスレッド書き込み

### 公式サイト

- **https://tkiyo1007-eng.github.io/soradama/** (2026年8月8日公開)。`index.html` と `assets/` をリポジトリのルートに置いている
- **`.nojekyll` が必須**。これが無いと GitHub Pages が Jekyll でビルドし、`README.md` がトップページとして配信されて `index.html` と `assets/` が 404 になる(実際に踏んだ)
- スクロール演出は IntersectionObserver + CSS transition。ただし **JS が動かないと永久に見えない作りは危険**なので、`.js` クラスが付いたときだけ伏せる + 3秒後に取りこぼしを必ず表示、の二重の保険をかけている
- 掲載画像は Web 用に軽量化(スクショ4枚+アイコンで計356KB)。アプリのスクショを差し替えたらここも更新する

### 今後の候補

- 品質(1.3.0調査で見つかった未対応分): 空の色が開きっぱなしだと時刻で更新されない / オフライン表示で過去の時刻が「今」と出る / 明るい空(雪・くもり日中)で白文字が読みにくい / Dynamic Type非対応(固定ptフォント) / 雨通知のBGAppRefreshTask化(現状アプリを開いた時のみ予約) / Watchが常に現在地固定でiPhoneの選択地点と食い違う / 風速・気圧・視程の単位換算が摂氏華氏に連動しない / DateFormatterを表示ごとに生成 / WeatherCacheの無制限肥大

- 機能: 空玉ずかんに日・夜バリエーションを追加(現在は天気種8種のみ)/ 空玉コレクションの共有導線を強化
- 成長施策: プレビュー動画✅ / X告知(手動)⏳ / レビュー依頼⏳

## ✅ 設定済み(このリポジトリに含まれるもの)

| 項目 | 内容 |
|---|---|
| バンドルID | `com.tkiyo1007.soradama`(+ `.widget` / `.watchkitapp`) |
| プライバシーマニフェスト | `PrivacyInfo.xcprivacy` を3ターゲットに同梱(位置情報: 機能目的・非紐付け・非トラッキング) |
| 暗号化の申告 | `ITSAppUsesNonExemptEncryption = NO`(HTTPSのみ=免除対象。アップロード毎の質問がスキップされます) |
| アプリアイコン | 1024px(iOS / watchOS 両方) |
| 位置情報の利用目的文 | 日本語で設定済み |
| プライバシーポリシー | リポジトリ直下 `privacy.html`(公開方法は手順4) |
| 対応デバイス | iPhone専用 + Apple Watch(iPad対応を外し、スクリーンショット準備を簡略化) |

## 手順1: Apple Developer Program に加入(未加入の場合)

- https://developer.apple.com/programs/ から加入(**年間 ¥14,800 程度**)。個人でOK
- 加入完了まで最大48時間ほどかかることがあります
- **無料アカウントのままではApp Storeに公開できません**

## 手順2: Xcodeでの最終確認

1. 3つのターゲットすべてで Team を選択(加入後は「kiyotaka takanishi (Individual)」のような有料チームを選ぶ)
2. (任意・推奨)App Group `group.com.tkiyo1007.soradama` を **AuroraWeather** と **AuroraWeatherWidgetExtension** の両方に追加 → ウィジェットが選択中の都市と同期します
3. 実機で一通り動作確認(位置情報許可 → 現在地表示 → 都市追加 → スワイプ → レーダー → ウィジェット → Watch)

## 手順3: App Store Connect でアプリを作成

1. https://appstoreconnect.apple.com/ → マイアプリ → 「+」→ 新規アプリ
2. 入力値:
   - プラットフォーム: **iOS**(watchOSは自動で含まれます)
   - 名前: **そらだま**(重複で弾かれたら「そらだま - 空玉」など)
   - プライマリ言語: **日本語**
   - バンドルID: **com.tkiyo1007.soradama**(初回はXcodeから一度アップロードすると選択肢に出ます)
   - SKU: `soradama001`(任意の管理用文字列)

## 手順4: プライバシーポリシーURLの用意

リポジトリ直下の `privacy.html` をGitHub Pagesで公開します:

1. GitHubの `soradama` リポジトリ → **Settings** → 左メニューの **Pages**
2. 「Build and deployment」の Branch を **main**、フォルダを **/(root)** にして **Save**
3. 1〜2分後に公開URL: `https://tkiyo1007-eng.github.io/soradama/privacy.html`
4. ブラウザで開けることを確認し、App Store Connect の「アプリのプライバシー」に設定

## 手順5: アプリのプライバシー(質問票)の回答

App Store Connect → アプリのプライバシー:

- 「データを収集しますか?」→ **はい**
- 収集するデータ: **正確な位置情報** のみ
  - 利用目的: **アプリの機能**
  - ユーザーのIDに紐付け: **いいえ**
  - トラッキングに使用: **いいえ**

(同梱のPrivacyInfo.xcprivacyと同じ内容です。ズレると審査で警告が出ます)

## 手順6: スクリーンショット撮影

シミュレータで **⌘S** を押すと保存されます。必要なもの:

- **iPhone 6.9インチ**(iPhone 16 Pro Maxシミュレータ): 最低2枚、推奨4〜6枚
  - おすすめ構成: ①晴れの都市 ②雨の都市(パーティクルが映える)③10日間予報まで見える画面 ④雨雲レーダー ⑤ウィジェット(ホーム画面)
- **Apple Watch**: Watchシミュレータで2枚以上(Watch対応を出す場合)

## 手順7: アーカイブとアップロード

1. Xcode上部のデバイス選択を **「Any iOS Device (arm64)」** に
2. メニュー **Product → Archive**
3. Organizerが開いたら **Distribute App → App Store Connect → Upload**(すべて既定でOK)
4. 15〜30分ほどで App Store Connect の「ビルド」に反映されます

## 手順8: メタデータ入力 → 審査提出

下の文案をそのまま使えます。ビルドを選択し、スクリーンショットとメタデータを入れて「審査へ提出」。初回審査は通常1〜3日です。

---

## 📝 ストア掲載文の文案

**サブタイトル(30文字以内)**
> 空そのものが動く、美しい天気予報

**プロモーションテキスト**(v1.0.2時点の現行版。審査なしで随時変更可)
> 気象庁の予報モデルに対応し、日本の天気をより正確に。雨の日は画面に雨が降る、開くたびに表情を変えるデザイン重視の天気アプリです。

**説明文**(v1.0.2時点の現行版。星・稲光など撤去済み演出の記述は削除済み)
> ■ そらだま(空玉)は、空そのものをデザインにした天気予報アプリです
>
> 晴れ・雨・雪・雷・霧——いまの空模様と時間帯に合わせて、アプリの背景が生きているように変化します。雨の日は雨粒が流れ、雪の日は雪が舞い、時間帯とともに空の色が移ろいます。
>
> ■ 主な機能
> ・現在の天気/24時間予報/10日間予報
> ・雨雲レーダー(1時間先までの予測アニメーション)
> ・複数都市をスワイプで切り替え
> ・日の出/日の入り・UV指数・風向・湿度・気圧・視程・体感温度
> ・ホーム画面ウィジェット(小・中)とロック画面ウィジェット
> ・雨の降り始めをお知らせする通知
> ・Apple Watch対応
> ・圏外でも最後に取得した天気を表示
> ・日本国内は気象庁(JMA)の予報モデルに対応
>
> ■ 安心して使えます
> ・アカウント登録不要/広告なし/トラッキングなし
> ・位置情報は天気の取得だけに使い、収集・保存しません
>
> 天気データ: Open-Meteo / 雨雲レーダー: RainViewer

**キーワード(100文字以内)**(v1.0.2時点の現行版)
> 天気,天気予報,雨雲,レーダー,ウィジェット,気温,降水確率,週間天気,台風,おしゃれ,かわいい,きれい,デザイン,シンプル

**サポートURL**
> https://github.com/tkiyo1007-eng/soradama(専用ページを作る場合は差し替え)

**年齢制限**: 4+ / **価格**: 無料 / **カテゴリ**: 天気

---

## ⚠️ 公開前に知っておくべき注意点

1. **Open-Meteoのライセンス**: 無料APIは**非商用利用**が条件です。無料アプリ(広告・課金なし)での公開は一般に問題ありませんが、将来課金や広告を入れる場合は有料プラン(月€29〜)への加入が必要です。
2. **RainViewer**: 無料利用可ですが、アプリ内にクレジット表記が必要です(実装済み: 画面下部の「データ提供」表記)。
3. **審査での指摘がありがちな点**: 位置情報許可を拒否してもアプリが使えること(→東京フォールバック実装済み)、プライバシーポリシーURLが開けること。
4. **バンドルIDは公開後変更できません**。`com.tkiyo1007.soradama` 以外にしたい場合は提出前に変更してください。

## 📝 初回審査で実際に却下された内容と対応(v1.0、ビルド1〜7)

天気アプリはApple純正「天気」アプリとの類似を理由に**5.2.5 (Legal: Intellectual Property)** で3回連続却下された。個別パーツの修正だけでは不十分で、最終的に解決したのは以下の対応。今後似たアプリを作る際は、最初からこれらを避けるとよい。

1. **ビルド3**: Watchアイコンの背景色・天気アイコンのSF Symbols依存・日の出アークの見た目を独自デザイン化 → **却下(同じ理由)**
2. **ビルド4**: 詳細情報を2列固定グリッド→横スクロールカルーセルに変更、Apple純正にない「傘指数」を追加、磨りガラスカードの配色を独自トーンに変更 → **却下(同じ理由、Appleがスクリーンショット添付)**
3. **ビルド6**: スクリーンショットで指摘されたのは「日の出・日の入り」カードの**横棒トラック+丸いつまみ**という構図そのもの(Apple純正のSunriseカードと同一構図)。UV指数と同じ**リングゲージ**表現に統一 → **却下(同じ理由、再度スクリーンショット添付、今度は画面右上の発光オーブを指摘)**
4. **ビルド7**: 背景に置いていた「そらだま」オーブ演出(発光する円+光条、画面右上に配置)を**完全に撤去**。天候はヘッダー内の小さなアイコンのみで表現 → **5.2.5は解消**。ただし今度は**2.3.3 (Performance: Accurate Metadata)**でストアのスクリーンショットが最新UIと食い違っていると指摘され、撮り直して提出 → **通過**

**教訓**:
- Appleが却下メッセージにスクリーンショットを添付してくれた場合は、それをダウンロードして必ず確認する(「解決センター」の提出物詳細ページの「ダウンロード」リンクから)。定型文だけでは分からない、具体的にどのUI要素が問題かが一目で分かる。
- 天気アプリで「画面右上に発光する太陽/月/玉を置く」「横棒トラック上をつまみが動く日の出インジケーター」は、たとえ独自デザインのつもりでも**Apple純正との構図の一致**とみなされるリスクが高い。天候表現は控えめな小アイコンに留め、装飾的な発光オブジェクトは避けるのが安全。
- コード側の修正が終わっても、**ストア掲載用スクリーンショットの更新を忘れない**。UIを変えたら毎回スクリーンショットも撮り直して同期させる。

## 📝 v1.0.1〜1.0.2 の運用で得た追加の教訓(2026年7月)

### 雨雲レーダーのタイル上限(RainViewer 時代 / v1.5.1 で気象庁へ移行)
- RainViewer のタイル提供は**ズームレベル7まで**。z=8 以上は「Zoom Level Not Supported」というプレースホルダー画像が返ってくる(HTTP 200 なのでエラーでは検知できない)。
- `MKTileOverlay` に `maximumZ = 7` を設定すること。未設定や大きすぎる値だと、実機(高解像度)でプレースホルダーがそのまま地図に表示される。**シミュレータでは画面解像度の違いで再現しないことがある**ので、タイル系の修正は必ず curl で実際のタイルを取得して上限を実測する。
- 512px タイル(`/512/{z}/{x}/{y}/`)を使うと同じズームレベルで解像度が2倍になる(上限は同じ z=7)。`tileSize = 512` とセットで指定。

### 英語対応で分かったこと(v1.6.0)

**ローカライズは「翻訳が無い＝日本語がそのまま出る」という壊れ方をする。** ビルドは通り、日本語で使っているぶんには何も起きないので、英語版を開くまで誰も気づけない。実際、最初のビルドでは画面の半分が日本語のまま出た。

**翻訳対象として抽出されないパターン(ぜんぶ実際に踏んだ)**
- `var label: String { return "晴れ" }` — **String を返すプロパティ**。SwiftUI が面倒を見てくれるのは `Text("...")` の中のリテラルだけで、String 型になった時点で対象外。`String(localized: "晴れ")` で包む。
- `GlassCard(title: "傘指数")` のように **String パラメータで受け渡すもの**。受け側を `LocalizedStringKey` に変えると、呼び出し側のリテラルが拾われるようになる。
- `return ("傘が必須です", Color.blue)` の**タプル**、`["澄みきった一日", ...]` の**配列リテラル**。
- `Text(isNow ? "今" : someString)` の**三項演算子**。全体が String に型推論されるため `Text` 任せでは訳されない。

**プレースホルダのキー不一致（いちばん気づきにくい）**
`Text("\(a)/\(b) コンプリート")` のように**補間を2つ以上**含む場合、xliff は `%1$lld/%2$lld コンプリート` と**位置指定子付き**で書き出すのに、実行時に引かれるキーは `%lld/%lld コンプリート` と**位置指定子なし**になる。片方しか登録しないと訳が引けず、英語版でも日本語のまま出る。**両方の形を .xcstrings に入れる**こと。
- ただし位置指定子を外すと**引数の順序が固定される**。英訳で語順を入れ替えている場合（`%1$@の%2$@` → `%2$@ on %1$@`）は、日付と気温が逆に出るなど静かに壊れる。**訳のほうを語順保持で書き直す**のが安全（実際に4件書き直した）。

**書式の落とし穴**
- `dateFormat = "H時"` のような**日本語専用の書式を直に指定しない**。`setLocalizedDateFormatFromTemplate("j")` を使えば、日本語で "15時"、英語で "3 PM" になる。`DateFormatter` をキャッシュしている場合は**キーにロケールも含める**こと(言語を変えて戻ったとき古い書式を使い続ける)。
- 逆に、**日付キーのフォーマッタ(`en_US_POSIX`)は絶対にロケール依存にしない**。`DailyOrb` のキーが変わるとコレクションが丸ごと消える。
- **曜日の頭文字をハードコードしない**（`["日","月","火",…]`）。`Calendar.veryShortWeekdaySymbols` を使い、`firstWeekday` で回転させる。地域によって週の始まりが月曜になる。
- **API に渡す言語も忘れない**。Open-Meteo の geocoding に `language=ja` を固定していたため、英語UIでも検索結果だけ「ロンドン / イングランド / 英国」と日本語で出ていた。対応外の言語は**英語**に落とす（日本語に落とすと読めない人のほうが多い）。

**CLI だけで完結させる手順**
```sh
# 1. 抽出（Xcode を開かなくても xliff で全キーが取れる）
xcodebuild -exportLocalizations -project AuroraWeather.xcodeproj \
  -localizationPath /tmp/l10n -exportLanguage en
# 2. /tmp/l10n/en.xcloc/Localized Contents/en.xliff の <source> が翻訳対象
# 3. 各ターゲットの Localizable.xcstrings に訳を書き込む（JSON を直接組み立ててよい）
```
ビルド時の自動抽出は derivedData にしか出ず、**元の .xcstrings には書き戻されない**(書き戻すのは Xcode IDE)。CLI 運用なら上の手順で回す。

`developmentRegion` は `ja` にしてある(日本語がソース、英語が翻訳先)。

### 共有スキームを追加した(v1.5.1)
以前は共有スキームが無く、CLI ビルドが環境に依存していた。`AuroraWeather.xcodeproj/xcshareddata/xcschemes/AuroraWeather.xcscheme` を追加済み。
- **空の `<TestPlans></TestPlans>` を書くとテストプラン方式とみなされ、`Testables` が無視される**。`xcodebuild test` が "not currently configured for the test action" で落ちるので、テストプランを使わないなら要素ごと消すこと。
- `storeKitConfigurationFileReference` で `Soradama.storekit` を指定してある(Xcode から実行したときだけ有効。`simctl launch` では効かない)。

### 気象庁 高解像度降水ナウキャストへの移行(v1.5.1)

RainViewer のライセンスが **personal / educational 用途に限られる**ため、商用利用が明示的に認められている気象庁の公開データへ移した。副次的に解像度が z=7 → z=10 に上がっている。

**エンドポイント**
- コマ一覧(実況): `https://www.jma.go.jp/bosai/jmatile/data/nowc/targetTimes_N1.json` — 5分刻み・過去3時間・**新しい順**
- コマ一覧(予測): 同 `targetTimes_N2.json` — 5分刻み・60分先まで・**新しい順**
- タイル: `.../nowc/{basetime}/none/{validtime}/surf/hrpns/{z}/{x}/{y}.png`(256px)

**実測で分かった癖(いずれも静かに壊れるので注意)**
- **偶数ズームしか配信していない**(z=4/6/8/10)。奇数を要求すると HTTP 200 が返るが中身は**完全に透明な PNG**(334バイト)。`MKTileOverlay` をサブクラス化し `url(forTilePath:)` で奇数を1段下の偶数へ読み替えている(`JMANowcastTileOverlay`)。
- 予測タイルのパスは実況と同じ **`/none/`**。`/immed/` だと透明タイルが返る。
- 時刻文字列 `yyyyMMddHHmmss` は**タイムゾーン表記のない JST**。UTC として読むと9時間ずれる。さらに `DateFormatter` は**空文字を渡すと nil ではなく2000年1月1日を返す**ため、桁数と数字チェックを先に入れること(テストで検出済み)。
- 提供範囲は**日本周辺のみ**(概ね緯度20〜50・経度118〜150)。海外の地点では雨雲が一切出ないため、範囲判定して案内文を出している。
- **出典表示が必須**(公共データ利用規約)。レーダー画面上部・設定画面・プライバシーポリシー・公式サイトの4箇所に記載済み。範囲外のときはデータを使っていないので出典を出さない。

**タイルが表示されないときの切り分け**
単に「その地域に雨が降っていない」だけのことが多い。curl でタイルを取得し、**334バイトなら空、それ以上なら雨あり**と判断できる。実際に検証する際は、先に全国を粗くスキャンして雨のあるタイル座標を見つけてから、その地点をアプリで開くのが速い。

### 気象庁(JMA)モデル対応(v1.0.1〜)
- 日本近域(緯度20〜47.5、経度122〜154)では `models=jma_seamless` を指定。精度が体感で大きく向上し、「精度が低い」レビューへの回答になる。
- **JMA モデルは UV 指数・視程・降水確率(傘指数の元)を返さない**。これらはモデル未指定の並行リクエストで補完し、時刻グリッドを揃えてマージする(`Shared/WeatherService.swift` 参照)。

### App Preview(プレビュー動画)の作り方
- 6.5インチ用の規定: **886×1920px・15〜30秒・30fps以下・音声トラック必須**(無音でよい)。
- シミュレータで `xcrun simctl io <UDID> recordVideo` で録画(1242×2688)→ ffmpeg で `scale=886:1920` + `anullsrc` の無音AACを付けて書き出し。
- ffmpeg が無い環境では `pip3 install imageio-ffmpeg` でスタティックバイナリが手に入る(Homebrew不要)。

### App Store Connect メタデータ編集の注意(実際に起きた事故)
- **説明文(概要)を「このバージョンの最新情報」と取り違えて上書きする事故が発生**(自動化スクリプトの検索条件が曖昧だったため)。v1.0.1 は概要が82字のリリースノートのまま公開されてしまった。
- 対策: 複数の textarea があるページで値を書き込む前に、**必ずラベル(このバージョンの最新情報/概要/プロモーション用テキスト)を厳密に特定**し、書き込み後に文字数カウンタで確認する。提出前に「提出物の下書き」パネルの内容も目視確認する。
- **プロモーション用テキストは審査なしで即時変更できる**(バージョン単位のフィールドなので、公開中バージョンと審査中バージョンの両方に設定が必要な点に注意)。
- 説明文・キーワード・スクリーンショット・プレビュー動画は**バージョンに紐付く**ため、変更にはそのバージョンの再提出が必要。審査待ち(未着手)ならキャンセル→修正→再提出のロスは小さいが、「審査中」に入ってからのキャンセルは列に並び直しになる。

### Xcode プロジェクトにターゲットを手で足すときの注意(v1.2.1)

`project.pbxproj` を直接編集してターゲットを追加する場合、最低限これだけ必要:
PBXFileReference(`.appex`)/ PBXFileSystemSynchronizedRootGroup(ソースフォルダ)/ PBXNativeTarget /
Sources・Frameworks・Resources の各フェーズ / XCBuildConfiguration ×2 と XCConfigurationList /
親ターゲット側の PBXCopyFilesBuildPhase(dstSubfolderSpec = 13)と PBXTargetDependency /
**PBXProject の `targets` 配列への追加**(これを忘れると `xcodebuild -list` に出てこない)。
`Info.plist` は PBXFileSystemSynchronizedBuildFileExceptionSet で除外しないとリソースとして二重コピーされる。
編集後は `plutil -lint project.pbxproj` と `xcodebuild -list` で検証する。

**バンドルIDに `.complication` は使えなかった**: Developer Portal 側で
`ENTITY_ERROR.ATTRIBUTE.INVALID / not available` になり App ID を登録できず署名に失敗する。
`com.tkiyo1007.soradama.watchkitapp.widget` に変更して解決。
なお watchOS 拡張のバンドルIDは Watch アプリのバンドルIDを接頭辞にする必要がある。

### SwiftUI で踏んだ落とし穴(v1.2.0 まで)
- **`ImageRenderer` は `TimelineView` の中身を描画しない**。壁紙の書き出しで雨粒・雪・光の帯がすべて消えた。静止画が必要な場面は、時刻を引数で受けて `TimelineView` を通さず直接 `Canvas` で描く経路を用意する。
- **巨大フォントの `Text` は `lineLimit` を明示しないと省略される**。108pt の気温が「2...」と表示された(`lineLimit(1)` + `minimumScaleFactor` + `fixedSize` で解消)。
- **`Int` に丸めた文字列はカウントアップできない**。`withAnimation` で `Double` の State を変えても中間値は描画されず、`contentTransition(.numericText())` は桁数変化で破綻する。経過時間から進捗を計算して `TimelineView` で毎フレーム描き直す。
- **`String.hashValue` はプロセスごとに変わる**(ハッシュフラッディング対策)。「同じ日は同じ見た目」のような決定的な結果が必要なら FNV-1a 等の安定ハッシュを自前で持つ。
- **App Group を使った設定共有を忘れやすい**。アプリ本体に設定 UI を足したら、ウィジェット・Watch・他画面にも反映されるか必ず確認する(単位切替で実際に漏れた)。

### その他
- 審査承認→自動リリースは数分で App Store に反映される。公開中バージョンの確認は `https://itunes.apple.com/lookup?id=6788443049&country=jp` が手軽(キャッシュ回避にランダムクエリを付ける)。
- 概要・スクリーンショットの内容が実装と食い違うと 2.3.3 で却下される。**機能を削除したときは説明文の該当記述も削る**(「夜には星がまたたきます」は背景演出撤去に合わせて削除済み)。

### スクリーンショット/動画のドラッグ&ドロップ時の重複に注意(v1.0.3)
- ユーザーにドラッグ&ドロップを依頼すると、Desktop上の**古いファイルも一緒に選択されて一緒にアップロードされる**ことがある(例: 新しい `_v2.mp4` に加えて古い `preview.mp4` も残っていて両方入った)。
- アップロード後は必ず「アプリプレビュー数: N/3」「スクリーンショット数: N/10」の**件数**を確認し、想定より多い場合は一覧(`read_page`でlistitemを見る)で古いファイル名を特定して個別に削除する。
- 依頼前にデスクトップ上の旧ファイルを削除しておくか、ファイル名にバージョン番号を付けて明確に区別すると事故が減る。
